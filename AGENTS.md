# Codex instructions

- In this repository, do not run any Python or Node.js commands on the host machine. Any command that invokes Python, Node.js, npm, pnpm, yarn, npx, or related tooling must be run inside a running Docker container.
